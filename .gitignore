__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.env.local
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
uploads/
.DS_Store
*.egg-info/
node_modules/
dist/
build/
.pytest_cache/
.coverage
htmlcov/

# Filled seed files contain real credentials — only templates are committed
seed/*.yaml
!seed/*.template.yaml
seed/reddit_post.md

# Instruments overlay has no secrets (just Cam's product data) — tracked on purpose
!seed/instruments_overlay.yaml

# Claude Code <-> Claude Web relay handoff (session-specific, not portable)
REVIEW_NEEDED.md
CC_RESPONSE.md
relay/relay.log
