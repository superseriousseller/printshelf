# STATE — Where the Project Is Right Now
Last updated: 2026-07-20

PrintShelf is a 3D-print tracker (log prints, filaments, printers; public maker profiles; affiliate-monetized filament Buy-links) — sibling project to SS Book Tracker, built solo by Cam. FastAPI + SQLAlchemy/Alembic backend, Jinja2/HTMX-style server-rendered dashboard (no JS framework), PostgreSQL on Railway (SQLite locally), R2-backed uploads, freemium with a Stripe Pro tier (free caps: 50 prints / 10 filaments / 10 collections, enforced centrally in `backend/limits.py`). Live at printshelf.app (staging at staging.printshelf.app, auto-deploy from the `staging`/`main` branches on Railway). Branch state (2026-09-02): `staging` == `main` == `19a6cbb`, fully reconciled (0 ahead / 0 behind). Session 33's audit fixes were QA'd and merged long ago (`7bf0ae7`); the subsequent affiliate/config commits ship straight to `main` (out of the customer-facing QA Gate's scope — see CLAUDE.md).

## Working / Shipping
- **Core tracking loop**: printers, filaments, prints (with photos, slicer settings, cost tracking, ratings, categories), public per-user profiles at `/@username`, Explore/Feed/Search with facets (material/brand/printer) and time-decayed "Trending" + "Most liked" sorts.
- **Auto-import pipeline** (the main growth lever this year): URL import from Printables/Thingiverse/Cults3D/MakerWorld/MyMiniFactory, a slicer post-processing script (`tools/printshelf_postprocess.py` → `POST /api/prints/ingest`) for Bambu/Orca/Prusa, and a keyless mobile share flow (`GET /share?url=` via an iOS Shortcut) — all deduped by canonical model URL.
- **Filament capture flow** (2026-07-12, `34919d1`, live on prod): 3-tier picker on the print form — your filaments → community-sourced suggestions (cross-user distinct combos, no new table) → inline quick-add — plus a dismissible post-save nudge on `/dashboard/prints`. Built specifically because filament logging was the funnel's real bottleneck (~3 of 71 users were logging filaments, starving the affiliate Buy-link revenue this app is monetized on).
- **"Preview in Real Filament"** — client-side WebGL studio (`/dashboard/preview` + public no-login `/preview`) rendering a sample model or an ephemeral (never-persisted) uploaded STL in a real filament's color/finish, wired to affiliate Buy-links. Fully launched and live (`FILAMENT_PREVIEW_ENABLED` set on prod).
- **Growth/retention infra**: Google OAuth login, PWA install (manifest + service worker + offline page, confirmed installable on iPhone), Collections (named print groups), likes/views engagement loop, user data export (JSON/CSV), affiliate disclosure footer, Chrome extension (separate repo) for capturing filament purchases from Bambu/Polymaker/SUNLU/FlashForge/etc. product pages.
- **Latest shipped fix** (`6e1ebad`, 2 days ago): print detail's "View on X" link now uses a shared `platform_display_name()` helper (`backend/import_service.py`) recognizing 7 platforms (was 4 hardcoded) plus a domain-name fallback for anything else; also added the missing `myminifactory` value to the `SourcePlatform` enum (API/docs had drifted — `/developers` documented it but the enum 400'd on it).
- **QA discipline**: an 85-check automated suite (`backend/scripts/qa.py`) plus a Playwright visual-audit script (`backend/scripts/visual_audit.py`) run before every merge to main; CLAUDE.md's "Customer-Facing QA Gate" requires staging deploy → QA script → explicit Cam go-ahead before merging.

## Known issues / tech debt
- **2 uncommitted, untracked paths — not part of this commit, Cam's call whether to add them:**
  - `.claude/` (`.claude/settings.json`, `.claude/scheduled_tasks.lock`) — local Claude Code config/scheduler state.
  - `relay/` (`relay.py`, `README.md`, `ensure_relay.sh`, `requirements.txt`, `.env`) — a ~19KB local relay script plus its own `.env`; supports the "Relay Protocol" human-review loop CLAUDE.md describes at the bottom of the file. Contains a `.env` — worth a deliberate look before ever adding it, in case it holds a secret that shouldn't land in git.
- **Staging/main: reconciled (2026-09-02)** — `staging` == `main` == `19a6cbb`. Session 33's audit fixes were merged to main via `7bf0ae7` and are live on prod. No divergence outstanding.
- **Staging data pollution**: staging's Explore/Feed and Cam's real `@PluggedIn3d` profile are heavily seeded with leftover automated-QA test prints ("QA Dragon", "Bug Fix QA — Violin Print", etc.) from past sessions — flagged in CLAUDE.md as needing Cam's go-ahead before bulk deletion since it touches his real account.
- **Affiliate refs still unset**: `BAMBU_AFFILIATE_REF`, `POLYMAKER_AFFILIATE_REF`, `MATTERHACKERS_AFFILIATE_REF` are not configured on prod — Buy-links to those stores work but earn nothing until Cam signs up for the programs. `AMAZON_AFFILIATE_TAG` is set and earning.
- **Deferred scale fix**: Explore's material/brand facets scan `Print.filament_ids` JSON in-memory rather than using a proper `print_filaments` association table — acceptable at current scale, explicitly deferred rather than forgotten.
- **`CommunityFilament` model**: confirmed dead code now that the community-suggestion picker reuses `affiliate.build_preview_catalog()` instead — logged for a future cleanup migration, not yet removed.
- Root-level `CC_RESPONSE.md` and `REVIEW_NEEDED.md` are artifacts of the CLAUDE.md "Relay Protocol" (human-review loop for AI-proposed decisions) — not app code, but worth knowing they exist if they look like stray files.
- CLAUDE.md itself is ~76KB/287 lines of dense session history — useful as an operating log but past due for the "compress Done, drop bugs after 2 sessions" hygiene rule it sets for itself (Standing Rules → File Hygiene).

## Roadmap / suggested next steps
- **Get session 33's audit fixes Cam-QA'd and merged to main** — they're sitting on staging waiting on the explicit go-ahead the QA Gate requires; this is the most immediate unblock.
- **Configure the pending affiliate refs** (Bambu/Polymaker/MatterHackers) — per CLAUDE.md's own prioritization notes, this is near-zero engineering work that unblocks revenue on Buy-links that are already rendering and already getting clicked.
- **Re-pull the funnel metrics** (`GET /api/metrics`) now that a few days have passed since the filament-capture flow shipped (2026-07-12) — CLAUDE.md flags this explicitly as the open question: did attach rate move past ~3/71 users, or does the picker/prompt need to be more prominent / the community-suggestion pool need to be richer?
- **Decide on the staging QA-test-print cleanup** — it's polluting Explore/Feed on staging and mixed into Cam's real profile; low-risk to fix once Cam gives the go-ahead, and it's actively making staging QA screenshots/demos look worse than prod.
- **Start a STATE.md-style roadmap doc** (this file) as the durable, high-signal companion to CLAUDE.md's session-by-session log — CLAUDE.md is excellent detailed history but this doc is meant to be the fast "where are we, what's broken, what's next" read, matching the pattern already established in the assistant (TECHDEBT.md) and social_dashboard (STATE.md/PROJECT_AUDIT.md/HANDOFF_DOC.md) sibling projects.
- **Decide whether `relay/` and `.claude/` belong in git** — currently untracked; if the relay script is meant to be a durable part of the workflow (not just a local experiment), it should either be committed or explicitly gitignored rather than left dangling as perpetually-uncommitted.
